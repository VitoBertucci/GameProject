package com.example;

public class Cards {
    // for every territory
    // generate a random number (assigned to a territory)
    // set this territory to property of current player
    // put the idexed "card" at end of the list
    // repeat steps until done
}
